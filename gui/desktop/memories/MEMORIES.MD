# Memories

- Created initial Tauri 2 + React + TypeScript scaffold in `gui/desktop`.
- Hub API client currently expects `GET /status` and `POST /pairing` on local hub.
- Added theme presets through CSS variables and `data-theme` switching.
- Rust commands are placeholders for clipboard, screenshot, and notifications.
