export type ThemePreset = "default" | "fallout" | "cyberpunk" | "matrix";

export const THEME_PRESETS: ThemePreset[] = [
  "default",
  "fallout",
  "cyberpunk",
  "matrix",
];

export const DEFAULT_THEME: ThemePreset = "default";
